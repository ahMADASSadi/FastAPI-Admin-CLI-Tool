"""
This file is automatically generated from fastapi-admin.

other custom app can be ancluded in the routes list and gets accessed with the defiend path
"""

from fastapi import FastAPI
from {{project_name}}.config.settings import lifespan


app = FastAPI(title="{{project_name}}",lifespan=lifespan)


routes=[

]

app.include_router(*routes)


@app.get("/")
async def read_root():
    return {"message": "Welcome, this is the mighty FastAPI!"}