"""
This file is automatically generated by fastapi-admin.

other custom app can be included in the routers list and gets accessed with the defiend path

"""

from fastapi import FastAPI
from .settings import lifespan


app = FastAPI(title="{{project_name}}",lifespan=lifespan)


routers=[

]

[ app.include_router(router) for router in routers]


@app.get("/")
async def read_root():
    return {"message": "Welcome, this is the mighty FastAPI!"}